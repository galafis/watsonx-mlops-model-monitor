apiVersion: v1
kind: Service
metadata:
  name: watsonx-mlops-monitor
  labels:
    app: watsonx-mlops-monitor
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: watsonx-mlops-monitor
